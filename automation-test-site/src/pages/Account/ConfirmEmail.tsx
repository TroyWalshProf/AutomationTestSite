import * as React from "react";
import { Layout } from "../../components/shared/layout";

const ConfirmEmail = (props: any) => {
  return (
    <Layout title="Confirm Email">
      <h2>ViewBag.Title.</h2>
      <div>
        <p>
          Thank you for confirming your email. Please Html.ActionLink("Click
          here to Log in", "Login", "Account", routeValues: null,
          htmlAttributes: new id = "loginLink")
        </p>
      </div>
    </Layout>
  );
};

export default ConfirmEmail;
